# Chapt 1

```bash
rustc main.rs
./main
> Hello, world!
```

## Commands

```bash
rustup docs --book # open tutorial book
cargo new # create new cargo project
cargo build # compile and build cargo project
cargo run # compile and run cargo project
cargo check # check without producing a binary for speedy error checking
cargo build --release # compile with optimizations
cargo update # update crates (packages)
cargo doc --open # build and read docs for your cargo project, including deps
```
